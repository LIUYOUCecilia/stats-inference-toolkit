# Codex Project Instructions

Keep context focused on the files needed for the current task. Start with core files such as `app.py`, `stats_inference_toolkit.py`, `ab_testers.py`, and `pdf_generator.py` before reading generated outputs or examples.

Keep responses concise. For implementation tasks, summarize only the key change, where it was made, and how it was verified. Avoid long process narration unless the user asks for it.

When editing this project, avoid adding unnecessary comments, long explanations, or duplicated statistical theory in user-facing text. The app and PDF should stay business-friendly by default, with deeper methods reserved for the researcher report mode.
